# React + NestJS 可玩性广告制作系统开发路线图（含详细优先级与截止日期）

> 目标：先完成 **“UI 可视化摆放 + 导出固定架构 React+Phaser3 工程包（zip）”** 的闭环，再逐步增强编辑效率，最后为“蓝图式可视化编程”打底。  
> 假设：**单人全职开发（每周 5 天）**。如为兼职，可将时间线按比例拉长（例如每天 2–3 小时≈整体 *2–3 倍*）。

当前日期：**2026-01-18（周日）**  
计划启动日期：**2026-01-19（周一）**  
计划以“周日”为每周里程碑验收节点。

---

## 0. 总体原则（优先级排序）

1. **一致性优先**：导出工程运行结果必须与画板一致（至少图片节点 0 偏差或 ≤1px）。
2. **闭环优先**：先跑通“编辑 → 导出 zip → 运行看效果”，再做体验增强。
3. **生成区隔离**：固定模板工程只允许覆盖 `src/generated/**`、`src/assets/**`。
4. **IR 先行**：内部一定要有 IR（中间表示），导出是 IR → 代码生成（不是导出 JSON 给用户）。
5. **可回归**：尽早引入最小化“截图对比”或可复现检查，避免后期一致性返工。

---

## 1. 目录与工程组织（第一天就定死）

### 1.1 Monorepo 推荐结构（pnpm workspace）
```
repo/
  apps/
    editor/                   # React 编辑器（Vite + MUI + Konva）
    server/                   # NestJS（项目、资源、导出、模板管理）
  packages/
    ir/                       # IR 类型 + Zod 校验 + 版本
    codegen/                  # IR -> Phaser/TS 生成器（纯 TS 库）
  templates/
    playable-react-phaser3/   # 固定模板工程（可运行）
  storage/                    # 本地开发用存储（assets / exports），可 gitignore
```

### 1.2 关键约定（必须写进 README）
- 生成区：`templates/playable-react-phaser3/src/generated/**`（导出覆盖）
- 资源区：`templates/playable-react-phaser3/src/assets/**`（导出覆盖）
- 模板固定区：其他所有目录导出器 **不得改写**
- 导出工程运行命令（模板里固定）：
  - `pnpm i`
  - `pnpm dev`
  - `pnpm build`
  - `pnpm preview`


---

## 1.3 导出 React 代码包结构（Template 产物结构，固定）

> 说明：每次“导出”得到的 zip 解压后，应包含如下 **固定目录结构**。  
> 其中 **`src/game/**` 负责 Phaser3 运行时逻辑（场景/组件/事件），`src/assets/**` 放素材，`src/config/**` 放配置；导出器只会覆盖约定的生成区与资源区（详见第 1.2 节约定）。

```
src/
├─ game/
│  ├─ scenes/        # 游戏场景（例如：MainScene、UIScene、PreloadScene 等）
│  ├─ components/    # 游戏组件（可复用 UI/交互/动画组件）
│  └─ EventBus.ts    # 事件系统（Scene/组件间通信）
├─ assets/           # 资源文件（图片、字体、音频等；导出器会拷贝到这里）
└─ config/           # 配置文件（分辨率、scale 策略、常量、开关等）
```

> 备注：如果你采用“生成区隔离”的方式（推荐），可以在 `src/game/` 里固定好接入点（例如 `MainScene` 调用 `src/generated/*`），并把所有导出生成内容放到 `src/generated/**`（不在上面树中展开），从而保证模板结构长期稳定。

---

## 2. 时间线总览（10 周主线 + 2 周蓝图预备）

### 2.1 主线 10 周（闭环 + 可用 + 专业化 + 稳定）
- **Week 1–2**：模板工程 & 后端导出骨架
- **Week 3–4**：编辑器 MVP + 端到端导出闭环（图片节点）
- **Week 5–6**：项目保存/资源管理 + Undo/Layer（变得能用）
- **Week 7–8**：效率工具（多选/对齐/吸附）+ 预览策略
- **Week 9–10**：文本节点策略 + 回归测试 + 发布打包

### 2.2 蓝图预备 2 周（可选）
- **Week 11–12**：引入 React Flow、定义蓝图 IR 雏形、编译链路占位

---

## 3. 逐周详细计划（含交付物与验收）

> **每周固定节奏建议（单人更稳）：**
- 周一：拆任务 + 风险点清单 + 本周验收标准写清楚
- 周二–周四：实现核心功能（避免同时开太多分支）
- 周五：集成 + 修 bug + 写最小文档/脚本
- 周六：缓冲/回归（可选）
- 周日：里程碑验收（必须能跑通）

---

# Week 1（2026-01-19 ～ 2026-01-25）
## 目标：模板工程 Template 可独立跑起来，并预留 generated/assets 接入点

### 任务清单（周级）
1. 创建模板工程 `templates/playable-react-phaser3`（Vite + React + TS + Phaser3）
2. 固定 Phaser 初始化方式：`PhaserHost`、`gameConfig`、`MainScene`
3. 设计 generated 接入点：推荐 `createUI(scene)`（由 MainScene 调用）
4. 约定资源加载方式：`assets.manifest.ts` + `loadAssets(...)`
5. 模板 README：运行命令、目录约束、生成区覆盖规则

### 验收标准（周日必须达成）
- 模板工程：`pnpm i && pnpm dev` 可跑
- 修改 `src/generated/createUI.generated.ts` 会立刻影响画面（证明接入点正确）

---

# Week 2（2026-01-26 ～ 2026-02-01）
## 目标：NestJS 导出服务骨架 + “复制模板→写 generated→打 zip”最小闭环（手工 IR）

### 任务清单（周级）
1. 初始化 NestJS：TemplateModule / ExportModule
2. TemplateModule：定位模板路径、模板版本（先 v1）
3. ExportModule：写死假 IR 导出 zip（先不做 DB/项目）
4. `packages/codegen` 雏形：仅生成 Image 节点的 `createUI.generated.ts`
5. 下载接口：产物落 `storage/exports/<exportId>.zip`

### 验收标准（周日）
- HTTP 触发导出 → 下载 zip → 解压 → `pnpm dev` → 看到写死图片

---

# Week 3（2026-02-02 ～ 2026-02-08）
## 目标：IR（最小版）+ React 编辑器 MVP（导入图片、拖拽摆放、属性面板）

### 任务清单（周级）
1. `packages/ir`：IR v1（只做 Image）+ Zod 校验
2. `apps/editor`：Vite + React + MUI + Konva
3. 上传图片（Asset API）+ 画布上创建 Image 节点
4. 单选、拖拽移动、属性面板（x/y/w/h/origin/depth）

### 验收标准（周日）
- 画布拖拽与属性面板双向绑定稳定
- IR 校验通过才能保存/导出（至少按钮禁用）

---

# Week 4（2026-02-09 ～ 2026-02-15）
## 目标：真实 IR → 导出 zip → Phaser 还原（图片节点一致性达标）

### 任务清单（周级）
1. Export API 接收 IR（或从 editor 直接提交 IR）
2. codegen：IR → `assets.manifest.ts` + `createUI.generated.ts`
3. 资源复制到导出工程 `src/assets/**`
4. 统一坐标与 origin：Konva ↔ Phaser 对齐
5. 最小回归案例（固定 IR）

### 验收标准（周日）
- 图片节点：导出后位置/大小/层级与编辑器一致（目标 0 偏差，允许 ≤1px）
- zip 解压即可运行

---

# Week 5（2026-02-16 ～ 2026-02-22）
## 目标：项目保存/打开（持久化）+ 资源管理最小可用

### 任务清单（周级）
1. ProjectModule：创建/读取/保存项目（存 IR）
2. AssetModule：上传/读取/列表（可选：hash 去重）
3. 编辑器：项目列表、打开项目、保存（可选自动保存）
4. 导出改为 projectId（后端读取 IR + assets）

### 验收标准（周日）
- 关闭重开仍能打开项目并导出复现

---

# Week 6（2026-02-23 ～ 2026-03-01）
## 目标：Undo/Redo + 图层面板（Depth）→ 工具变得“真的能用”

### 任务清单（周级）
1. Command Stack：Move/Resize/UpdateProp/ReorderDepth
2. Layer 面板：排序、上移下移、置顶置底、隐藏、锁定
3. 导出层级一致性：depth → Phaser setDepth

### 验收标准（周日）
- 关键操作可撤销/重做
- 锁定节点不可拖拽

---

# Week 7（2026-03-02 ～ 2026-03-08）
## 目标：多选与对齐/分布工具（效率提升）

### 任务清单（周级）
1. 多选：Shift 多选 + 框选
2. 对齐：左右/水平居中/上下/垂直居中
3. 分布：水平/垂直等间距（可选：先做水平）
4. 多选拖拽 + Undo 支持

### 验收标准（周日）
- 多选对齐与撤销稳定可用

---

# Week 8（2026-03-09 ～ 2026-03-15）
## 目标：吸附（Snapping）+ 预览策略（分辨率/安全区）

### 任务清单（周级）
1. 吸附：画布中心线、边缘（必做）；吸附其他节点（可选）
2. 网格/辅助线显示
3. 画布预设：1080×1920、720×1280
4. Phaser scale 规则固化并与编辑器预览一致

### 验收标准（周日）
- 吸附不抖动、可控
- 切换画布尺寸不“乱”

---

# Week 9（2026-03-16 ～ 2026-03-22）
## 目标：Text 节点 v1（受限版）+ 导出一致性策略

### 任务清单（周级）
1. IR 扩展 TextNode：text/fontKey/fontSize/color/align（可选 maxWidth）
2. 编辑器 Konva.Text 渲染
3. 导出 Phaser Text 生成：统一 style、字体加载策略
4. 文本一致性用例：固定字体/字号/字符串

### 验收标准（周日）
- 受限条件下文本位置/边界误差可接受（建议 ≤1–2px）

---

# Week 10（2026-03-23 ～ 2026-03-29）
## 目标：稳定化、回归测试、可发布版本

### 任务清单（周级）
1. Demo 项目集（3–5 个）作为回归基准
2. 最低限度自动化（可选强烈建议）：Playwright 截图 + pixelmatch
3. 文档：安装/导出/模板升级/常见问题
4. 错误提示、临时目录清理、导出包过期清理

### 验收标准（周日）
- 任意 demo：导出 zip → 解压运行成功
- 文档齐全，可交付给他人使用

---

## 4. 蓝图预备（可选）

# Week 11（2026-03-30 ～ 2026-04-05）
## 目标：蓝图编辑器雏形（React Flow）+ 蓝图 IR 占位
- 引入 React Flow
- 节点最小集合：OnStart / OnClick / SetVisible
- 蓝图数据保存到项目（IR v2 或 logic 文档）
- 暂不编译复杂代码，仅打通“编辑→保存→加载”

# Week 12（2026-04-06 ～ 2026-04-12）
## 目标：蓝图编译链路“最小可跑”（占位编译）
- 蓝图图 → 生成 `logic.generated.ts`（仅输出事件绑定骨架）
- 模板接入 `logic.generated.ts`
- 导出工程能看到最小逻辑效果（点击按钮隐藏图片）

---

## 5. 关键里程碑（必须达成）

- **M1（2026-01-25）**：模板工程可运行 + generated 接入点可验证
- **M2（2026-02-15）**：编辑器摆图 → 导出 zip → Phaser 还原一致（图片节点）
- **M3（2026-03-01）**：Undo/Redo + Layer → 工具“可长期使用”
- **M4（2026-03-29）**：稳定化版本 + Demo 回归 + 文档齐全

---

## 6. 风险与缓冲策略（避免延期）

### 最高风险：一致性（编辑器 vs Phaser）
- 优先把 **图片节点** 做到一致后再开文本
- 每周都保留一个“基准 demo”用于对照

### 文本风险控制
- Week 9 才引入 Text
- 先做受限版：固定字体/字号/对齐，减少不可控差异

### 范围控制（单人必须做）
如果出现时间压力，按以下顺序砍掉（不影响闭环）：
1) 自动化截图 diff（先人工）
2) 吸附到其他节点（先只吸附中心线/边界）
3) 分布工具（先做对齐）
4) 文本高级特性（maxWidth/换行/描边阴影等）

---

# 7. 每周按天执行清单（全部周都列出，可直接照做）

> 说明：每周默认工作日为周一～周五；周六为缓冲；周日为里程碑验收。  
> 每天建议输出一个“可运行状态”或“可验证结果”，避免积压。

---

## Week 1（2026-01-19 ～ 2026-01-25）模板工程可运行

- **2026-01-19（周一）**：模板工程初始化  
  - [ ] 创建 `templates/playable-react-phaser3`（Vite + React + TS）  
  - [ ] 安装 Phaser3 依赖并确认可编译  
  - [ ] 写 `README.md` 框架（先列目录约束与运行命令占位）  
  - ✅ 当日验收：`pnpm dev` 能启动 React 页面（可先无 Phaser）

- **2026-01-20（周二）**：PhaserHost 与 Game 初始化  
  - [ ] 新建 `src/game/PhaserHost.tsx`（挂载容器 div/canvas）  
  - [ ] 新建 `src/game/gameConfig.ts`（先固定 width/height/scale 占位）  
  - [ ] 新建 `src/game/MainScene.ts`（create/preload 占位）  
  - ✅ 当日验收：页面出现 Phaser canvas（哪怕只是背景色）

- **2026-01-21（周三）**：确定 scale/坐标策略并固化  
  - [ ] 选择并写死 Phaser Scale 策略（如 FIT + CENTER_BOTH）  
  - [ ] MainScene 画一个基准参考（例如中心点小方块/十字）  
  - [ ] 写注释说明坐标系（(0,0) 左上，(w,h) 右下）  
  - ✅ 当日验收：缩放时 canvas 行为符合预期（不拉伸或按策略拉伸）

- **2026-01-22（周四）**：generated 接入点（createUI）  
  - [ ] 创建 `src/generated/createUI.generated.ts`（手写占位）  
  - [ ] MainScene `create()` 中调用 `createUI(this)`  
  - [ ] `createUI` 内添加一张临时图（可先用内置 sprite 或占位矩形）  
  - ✅ 当日验收：改 `createUI.generated.ts` 会立刻影响画面

- **2026-01-23（周五）**：资源 manifest 接入方式  
  - [ ] 创建 `src/generated/assets.manifest.ts`（手写占位）  
  - [ ] 写 `src/game/loadAssets.ts`（根据 manifest 执行 this.load.image/font 等）  
  - [ ] MainScene `preload()` 调用 `loadAssets(this, manifest)`  
  - ✅ 当日验收：通过 manifest 能成功加载 1 张图片并显示

- **2026-01-24（周六）缓冲/整理**  
  - [ ] 清理目录结构、补齐 README（固定区/生成区规则写清楚）  
  - [ ] 加一个 “模板回归” demo：确保 clone 后能跑  
  - ✅ 当日验收：README 的步骤按文档可复现

- **2026-01-25（周日）里程碑 M1 验收**  
  - [ ] 全新环境：`pnpm i && pnpm dev` 可运行  
  - [ ] 修改 generated 文件能影响画面  
  - [ ] 目录约束明确：只允许覆盖 generated/assets

---

## Week 2（2026-01-26 ～ 2026-02-01）导出服务最小闭环（假 IR）

- **2026-01-26（周一）**：NestJS 初始化与模块骨架  
  - [ ] 创建 `apps/server` NestJS 项目  
  - [ ] 建立模块：TemplateModule、ExportModule  
  - [ ] 配置环境：export 输出目录 `storage/exports`  
  - ✅ 当日验收：`GET /health` 正常返回

- **2026-01-27（周二）**：TemplateModule（读取模板）  
  - [ ] 能找到模板路径 `templates/playable-react-phaser3`  
  - [ ] 加 `templateVersion=v1` 常量与校验  
  - [ ] 提供 `copyTemplateTo(tempDir)` 工具函数  
  - ✅ 当日验收：调用方法可复制模板到临时目录

- **2026-01-28（周三）**：codegen 雏形（写死 IR 生成代码）  
  - [ ] 创建 `packages/codegen`（纯 TS）  
  - [ ] 输入：写死一个 Image 节点（x/y/origin/depth/key）  
  - [ ] 输出：字符串形式 `createUI.generated.ts`  
  - ✅ 当日验收：生成文件写入临时模板目录后可运行

- **2026-01-29（周四）**：打包 zip 与下载接口  
  - [ ] Export API：`POST /exports` 触发导出  
  - [ ] 生成 zip：`storage/exports/<id>.zip`  
  - [ ] `GET /exports/:id/download` 返回文件流  
  - ✅ 当日验收：能下载 zip 且解压后项目结构完整

- **2026-01-30（周五）**：端到端手工验证与错误处理  
  - [ ] 为导出增加错误提示（模板缺失、写文件失败）  
  - [ ] 写最小操作文档：curl 调用导出、如何运行 zip  
  - ✅ 当日验收：HTTP → zip → `pnpm dev` → 看到写死图片

- **2026-01-31（周六）缓冲**  
  - [ ] 清理临时目录策略（导出后删除 tempDir）  
  - [ ] 输出文件命名规则与日志记录  
  - ✅ 当日验收：连续导出 3 次都成功且不残留垃圾

- **2026-02-01（周日）里程碑验收**  
  - [ ] 一键导出 zip 可运行（假 IR）  
  - [ ] 模板复制、生成、打包链路稳定

---

## Week 3（2026-02-02 ～ 2026-02-08）编辑器 MVP（导入图片+拖拽+属性）

- **2026-02-02（周一）**：IR v1 定义（Image only）  
  - [ ] `packages/ir`：Canvas + ImageNode 类型  
  - [ ] Zod 校验：数值范围、必填字段、id 唯一（先弱校验也可）  
  - ✅ 当日验收：IR 能通过校验并可序列化/反序列化

- **2026-02-03（周二）**：编辑器工程初始化  
  - [ ] 创建 `apps/editor`（Vite + React + TS + MUI）  
  - [ ] Konva 基础画布：显示画布边界、缩放到窗口  
  - ✅ 当日验收：能看到空画布并缩放/拖动视图（可先不做拖动画布）

- **2026-02-04（周三）**：资源上传与预览  
  - [ ] 后端新增 Asset API（最小：上传 + 返回 URL/assetId）  
  - [ ] 编辑器实现上传按钮、能显示缩略图  
  - ✅ 当日验收：上传图片成功，前端拿到可展示的 preview

- **2026-02-05（周四）**：创建 Image 节点并渲染  
  - [ ] 点击资源 → 创建 ImageNode（默认居中）  
  - [ ] Konva 渲染该 Image（使用 HTMLImageElement）  
  - ✅ 当日验收：图片出现在画布上

- **2026-02-06（周五）**：拖拽与属性面板双向绑定  
  - [ ] 单选：点选出现选中框  
  - [ ] 拖拽更新 x/y  
  - [ ] 属性面板编辑 x/y/w/h/origin/depth 更新画布  
  - ✅ 当日验收：拖拽与面板修改互相同步，无明显抖动

- **2026-02-07（周六）缓冲**  
  - [ ] 修 Konva 坐标/缩放下的拖拽偏移问题  
  - [ ] 增加导出按钮占位（先打印 IR 到 console）  
  - ✅ 当日验收：拖拽在缩放情况下仍准确

- **2026-02-08（周日）里程碑验收**  
  - [ ] 生成可校验 IR  
  - [ ] 编辑器最小摆图体验可用

---

## Week 4（2026-02-09 ～ 2026-02-15）真实 IR 导出一致（图片）

- **2026-02-09（周一）**：导出 API 接入 IR  
  - [ ] `POST /exports` 支持接收 IR + asset 引用（临时方案）  
  - [ ] 后端导出前进行 Zod 校验（或同构校验）  
  - ✅ 当日验收：后端能拿到 editor 传来的 IR

- **2026-02-10（周二）**：生成 assets.manifest.ts  
  - [ ] 设计 manifest 格式（key/path/type）  
  - [ ] codegen 生成 `assets.manifest.ts`  
  - ✅ 当日验收：导出工程 preload 能加载 manifest 中的图片

- **2026-02-11（周三）**：生成 createUI.generated.ts（基于 IR）  
  - [ ] IR→Phaser：x/y/origin/depth/scale/displaySize 规则定死  
  - [ ] 输出节点创建顺序按 depth 排序  
  - ✅ 当日验收：导出工程 UI 出现且层级正确

- **2026-02-12（周四）**：资源复制到 src/assets  
  - [ ] 后端导出时把上传资源复制进导出工程固定目录  
  - [ ] manifest 路径指向正确（相对路径一致）  
  - ✅ 当日验收：导出工程无需依赖 server 的静态资源也能加载图片

- **2026-02-13（周五）**：一致性校准（Konva ↔ Phaser）  
  - [ ] 明确编辑器坐标系与 Phaser 坐标系一致规则  
  - [ ] 修正 origin（Konva offset/anchor ↔ Phaser setOrigin）  
  - ✅ 当日验收：图片节点位置/大小与编辑器一致（目标 ≤1px）

- **2026-02-14（周六）缓冲**  
  - [ ] 固定一个 IR demo case，重复导出 3 次对比一致  
  - [ ] 记录已定下的坐标/origin 规则到文档  
  - ✅ 当日验收：可复现，规则写清楚

- **2026-02-15（周日）里程碑 M2 验收**  
  - [ ] 编辑器摆图 → 导出 zip → 运行一致（图片节点）

---

## Week 5（2026-02-16 ～ 2026-02-22）项目持久化 + 资源管理

- **2026-02-16（周一）**：DB/Prisma 初始化  
  - [ ] 配置 Postgres + Prisma（或先 SQLite，但建议上 Postgres）  
  - [ ] 建表：Project（name/ir/json/version/updatedAt），Asset（path/hash/type）  
  - ✅ 当日验收：能创建一条项目记录并读取

- **2026-02-17（周二）**：Project API  
  - [ ] `POST /projects`、`GET /projects/:id`、`PUT /projects/:id`  
  - [ ] 保存 IR（带版本号）  
  - ✅ 当日验收：能保存并读取 IR

- **2026-02-18（周三）**：编辑器接入项目保存/打开  
  - [ ] 项目列表页（最简）  
  - [ ] 打开项目加载 IR 到画布  
  - ✅ 当日验收：刷新页面后仍能恢复项目

- **2026-02-19（周四）**：Asset 管理完善  
  - [ ] 上传资源保存到 `storage/assets`，DB 记录元信息  
  - [ ] 返回可用 preview URL  
  - ✅ 当日验收：同一项目可重复使用已上传资源

- **2026-02-20（周五）**：导出从 projectId 触发  
  - [ ] `POST /exports {projectId}`：后端读取 IR + assets 生成  
  - ✅ 当日验收：导出无需前端传 IR

- **2026-02-21（周六）缓冲**  
  - [ ] 自动保存（可选）：每 N 秒保存或 Ctrl+S  
  - [ ] 导出历史列表（可选）：存 export 记录  
  - ✅ 当日验收：基本稳定

- **2026-02-22（周日）里程碑验收**  
  - [ ] 项目可保存/打开/导出复现

---

## Week 6（2026-02-23 ～ 2026-03-01）Undo/Redo + Layer

- **2026-02-23（周一）**：Command Stack 基础框架  
  - [ ] 定义 command 接口：do/undo/redo  
  - [ ] 支持堆栈与快捷键  
  - ✅ 当日验收：一个简单命令可撤销/重做

- **2026-02-24（周二）**：Move/UpdateProp 命令接入  
  - [ ] MoveNode（拖拽）用 command 记录  
  - [ ] UpdateProp（面板改值）用 command  
  - ✅ 当日验收：移动与改值可撤销

- **2026-02-25（周三）**：Layer 面板 v1  
  - [ ] 列表显示节点（按 depth）  
  - [ ] 点击列表选中节点  
  - ✅ 当日验收：列表与画布选中同步

- **2026-02-26（周四）**：层级操作 + 锁定/隐藏  
  - [ ] 上移/下移/置顶/置底（ReorderDepth command）  
  - [ ] 隐藏（visible=false）  
  - [ ] 锁定（locked=true，阻止拖拽）  
  - ✅ 当日验收：层级操作可撤销，锁定生效

- **2026-02-27（周五）**：导出 depth 规则校验  
  - [ ] 确认 export 使用同一 depth 排序  
  - [ ] 处理 depth 冲突策略（如自动重排）  
  - ✅ 当日验收：导出层级与编辑器一致

- **2026-02-28（周六）缓冲**  
  - [ ] 修复 Undo 边界 bug（多次撤销、跨项目）  
  - ✅ 当日验收：连续撤销/重做稳定

- **2026-03-01（周日）里程碑 M3 验收**  
  - [ ] Undo/Redo + Layer 完整可用

---

## Week 7（2026-03-02 ～ 2026-03-08）多选 + 对齐/分布

- **2026-03-02（周一）**：多选模型与交互  
  - [ ] 选区状态：selectedIds[]  
  - [ ] Shift 多选  
  - ✅ 当日验收：能选中多个节点并高亮

- **2026-03-03（周二）**：框选  
  - [ ] Konva 画 selection rect  
  - [ ] rect 覆盖命中节点加入 selectedIds  
  - ✅ 当日验收：框选可用

- **2026-03-04（周三）**：多选拖拽  
  - [ ] 选中多个节点一起移动  
  - [ ] 单次操作形成一个 command（组合 command）  
  - ✅ 当日验收：多选移动可撤销

- **2026-03-05（周四）**：对齐工具  
  - [ ] 左/右/水平居中、上/下/垂直居中  
  - [ ] 形成可撤销 command  
  - ✅ 当日验收：对齐正确

- **2026-03-06（周五）**：分布工具（最小）  
  - [ ] 水平等间距（必做）  
  - [ ] 垂直等间距（可选）  
  - ✅ 当日验收：分布正确可撤销

- **2026-03-07（周六）缓冲**  
  - [ ] 修多选 + layer + undo 的边界问题  
  - ✅ 当日验收：复杂操作链不崩

- **2026-03-08（周日）里程碑验收**  
  - [ ] 多选/对齐/分布可用于真实摆 UI

---

## Week 8（2026-03-09 ～ 2026-03-15）吸附 + 预览

- **2026-03-09（周一）**：吸附规则设计  
  - [ ] 定义 snapping targets：画布中心线/边缘  
  - [ ] 定义阈值（例如 6px）  
  - ✅ 当日验收：文档写清规则 + 代码接口

- **2026-03-10（周二）**：实现中心线/边缘吸附  
  - [ ] 拖拽时计算最近吸附点并修正 x/y  
  - [ ] 显示辅助线（guide line）  
  - ✅ 当日验收：吸附可用且稳定

- **2026-03-11（周三）**：网格与可视化辅助  
  - [ ] 网格显示/隐藏  
  - [ ] 吸附到网格（可选）  
  - ✅ 当日验收：网格不影响性能

- **2026-03-12（周四）**：画布预设切换  
  - [ ] 预设尺寸列表（1080×1920、720×1280）  
  - [ ] 切换后画布缩放与中心对齐正确  
  - ✅ 当日验收：切换后仍能正常编辑/导出

- **2026-03-13（周五）**：与模板 scale 策略一致性检查  
  - [ ] 确认 Phaser scale 与编辑器预览一致  
  - [ ] 修正差异（必要时引入“基准坐标系”概念）  
  - ✅ 当日验收：同一 IR 在导出工程预览无明显偏差

- **2026-03-14（周六）缓冲**  
  - [ ] 修吸附抖动、边界吸附不准  
  - ✅ 当日验收：吸附体验达到可用

- **2026-03-15（周日）里程碑验收**  
  - [ ] 吸附 + 预设尺寸切换可稳定使用

---

## Week 9（2026-03-16 ～ 2026-03-22）Text v1（受限）

- **2026-03-16（周一）**：TextNode IR 设计  
  - [ ] 字段：text/fontKey/fontSize/color/align/maxWidth?  
  - [ ] 约束：字体来源策略（内置或上传）  
  - ✅ 当日验收：Zod 校验通过 + 默认值策略明确

- **2026-03-17（周二）**：编辑器 Text 渲染与编辑  
  - [ ] Konva.Text 渲染  
  - [ ] 属性面板可编辑 text/fontSize/color/align  
  - ✅ 当日验收：编辑器内文本显示正常

- **2026-03-18（周三）**：模板字体加载策略固化  
  - [ ] 模板内置字体（推荐）或 webfont loader（模板固定实现）  
  - [ ] manifest 支持 font 条目（如需要）  
  - ✅ 当日验收：导出工程能稳定加载字体

- **2026-03-19（周四）**：codegen 生成 Phaser Text  
  - [ ] 生成 `scene.add.text(x,y,text,style).setOrigin().setDepth()`  
  - [ ] align、wordWrap（如启用 maxWidth）规则定死  
  - ✅ 当日验收：导出文本显示位置正确（初版）

- **2026-03-20（周五）**：文本一致性测试用例  
  - [ ] 3 个固定 case：短文本/长文本/不同 align  
  - [ ] 记录截图基准（人工即可）  
  - ✅ 当日验收：误差在可接受范围（建议 ≤1–2px）

- **2026-03-21（周六）缓冲**  
  - [ ] 修基线/对齐差异、字体加载时机 bug  
  - ✅ 当日验收：重启后依旧稳定

- **2026-03-22（周日）里程碑验收**  
  - [ ] Text v1 受限版可用于真实项目

---

## Week 10（2026-03-23 ～ 2026-03-29）稳定化 & 可发布

- **2026-03-23（周一）**：基准 demo 项目集  
  - [ ] 建 3–5 个 demo 项目（纯图片/复杂层级/含文本）  
  - ✅ 当日验收：每个 demo 可导出并运行

- **2026-03-24（周二）**：导出稳定性与清理  
  - [ ] 导出 tempDir 生命周期管理  
  - [ ] 错误日志结构化（exportId 关联）  
  - ✅ 当日验收：导出失败时能定位原因

- **2026-03-25（周三）**：最小自动化（可选强烈建议）  
  - [ ] Playwright：启动导出工程并截图  
  - [ ] pixelmatch：与基准截图对比  
  - ✅ 当日验收：至少 1 个 demo 走通自动化

- **2026-03-26（周四）**：完善文档  
  - [ ] 安装运行、导出说明、模板升级说明  
  - [ ] 常见问题（坐标/origin/depth/字体）  
  - ✅ 当日验收：新人按文档可从零跑通 demo

- **2026-03-27（周五）**：产品级打磨  
  - [ ] 编辑器错误提示（缺资源、保存失败）  
  - [ ] 导出状态提示（loading/success/fail）  
  - ✅ 当日验收：体验无明显阻塞

- **2026-03-28（周六）缓冲**  
  - [ ] 回归全 demo  
  - [ ] 修最后一批 bug  
  - ✅ 当日验收：连续导出 5 次无异常

- **2026-03-29（周日）里程碑 M4 验收**  
  - [ ] 可交付版本：导出 zip 稳定、文档齐全、demo 回归通过

---

## Week 11（2026-03-30 ～ 2026-04-05，可选）蓝图雏形

- **2026-03-30（周一）**：React Flow 接入与基础画布  
- **2026-03-31（周二）**：节点类型定义（OnStart/OnClick/SetVisible）  
- **2026-04-01（周三）**：连线规则与参数面板（最小）  
- **2026-04-02（周四）**：蓝图数据保存到项目（logic 文档或 IR v2）  
- **2026-04-03（周五）**：加载与渲染回归  
- **2026-04-04（周六）缓冲**  
- **2026-04-05（周日）验收**：能编辑并保存一张蓝图图

---

## Week 12（2026-04-06 ～ 2026-04-12，可选）蓝图最小编译占位

- **2026-04-06（周一）**：定义蓝图→代码生成接口（占位）  
- **2026-04-07（周二）**：生成 `logic.generated.ts` 骨架（事件绑定框架）  
- **2026-04-08（周三）**：模板接入 logic（MainScene 调用）  
- **2026-04-09（周四）**：实现最小 demo：点击按钮隐藏图片  
- **2026-04-10（周五）**：回归与错误提示  
- **2026-04-11（周六）缓冲**  
- **2026-04-12（周日）验收**：导出工程具备最小可视化逻辑效果
